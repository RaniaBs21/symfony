{% extends 'basefront.html.twig' %}

{% block body %}
    <h1>Edit Reclamation</h1>

    {{ include('reclamation/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_reclamation_front_index2') }}">back to list</a>

    {{ include('reclamation/_delete_form2.html.twig') }}
{% endblock %}
