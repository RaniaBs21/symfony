<form method="post" action="{{ path('app_reponse_q_delete', {'idRep': reponse_quiz.idRep}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reponse_quiz.idRep) }}">
    <button class="btn">Delete</button>
</form>
