
<style>.card {
        background-color: #f8f9fa;
        border: 1px solid #dee2e6;
        border-radius: 0.25rem;
        margin-bottom: 1rem;
    }

    .card-body {
        padding: 1rem;
    }

    .form-group {
        margin-bottom: 1rem;
    }

    label {
        display: block;
        font-weight: bold;
    }

    input[type="hidden"] {
        display: none;
    }

    button {
        margin-right: 0.5rem;
    }

    button[type="reset"] {
        background-color: #6c757d;
        color: #fff;
    }

    button[type="submit"] {
        background-color: #d09900;
        color: #fff;
    }
</style>

<div class="row">
    <div class="col-lg-6">
        <div class="d-flex align-items-center vh-100">
            <div class="card col-sm-10 mx-auto text-center">
                <div class="card-body">

                    <form method="post" action="{{ path('app_question_q_delete', {'idQuest': question_quiz.idQuest}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
                        <div class="form-group">
                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ question_quiz.idQuest) }}">
                            <label for="delete-confirm">Are you sure you want to delete this item?</label>
                        </div>

                        <button class="btn btn-secondary" type="reset">Cancel</button>
                        <button class="btn btn-danger" type="submit">Delete</button>

                    </form>

            </div>
        </div>
    </div>
    </div>
</div>

